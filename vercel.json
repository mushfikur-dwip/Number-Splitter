{
  "buildCommand": "PORT=3000 BASE_PATH=/ pnpm --filter @workspace/number-splitter run build",
  "outputDirectory": "artifacts/number-splitter/dist/public",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
